<div class="top-hero-wrapper text-center">
  <div class="container top-hero-container">
    <h1 class="text-white">ようこそ、GreatConnect へ</h1>
    <h2 class="text-white">このアプリは名産物を販売したい人と買いたい人を繋げるアプリです</h2>
    <div class="row">
      <% if logged_in? %>
        <div class="col-md-offset-4 col-md-4 text-cetner text-white">
          <h3>現在ログインユーザー：<%= current_user.name %></h3>
        </div>
      <% else %>
        <div class="col-md-offset-3 col-md-3">
          <%= link_to '登録する', new_user_path, class: 'btn btn-block btn-white' %>
        </div>
        <div class="col-md-3">
          <%= link_to 'ログインする', login_path,class: 'btn btn-block btn-white' %>
        </div>
      <% end %>
    </div>
  </div>
</div>
